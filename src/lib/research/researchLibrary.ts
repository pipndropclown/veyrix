import type{ResearchComparison,ResearchSummary}from"@/types/research";
export function compareResearchSummaries(summaries:readonly ResearchSummary[]):ResearchComparison{if(summaries.length<2||summaries.length>3)throw new Error("Select two or three research runs");const ids=new Set(summaries.map(summary=>summary.researchRunId));if(ids.size!==summaries.length)throw new Error("Research runs must be unique");return{runIds:[...ids],rows:summaries.map(({researchRunId,timeframe,anchor,topStrategy,temporalResearchScore,anchorConsistency,multiRegimeRobustness,averageOosReturnPercent,matchedBenchmarkBeatRatePercent,temporalReliability})=>({researchRunId,timeframe,anchor,topStrategy,temporalResearchScore,anchorConsistency,multiRegimeRobustness,averageOosReturnPercent,matchedBenchmarkBeatRatePercent,temporalReliability}))}}
