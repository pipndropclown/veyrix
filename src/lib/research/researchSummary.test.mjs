import assert from"node:assert/strict";import test from"node:test";import{createResearchSummary,sanitizeForJson,serializedBytes}from"./researchSummary.ts";import{arena}from"./researchTestFixtures.mjs";
const make=()=>createResearchSummary({runId:"VRX-aaaaaaaaaaaaaaaaaaaaaaaa",createdAt:"2026-01-01T00:00:00Z",timeframe:"180D",anchor:"2026-01-01T00:00:00Z",datasetFingerprint:"datasetabcdefghijkl",resultFingerprint:"resultabcdefghijkl",durationMs:12,arena:arena()});
test("summary contains required metrics",()=>{const s=make();assert.equal(s.topStrategy,"momentum");assert.equal(s.researchScore,70);assert.equal(s.averageOosReturnPercent,1)});
test("summary excludes heavy detailed arrays",()=>{const s=make();assert.equal("entries"in s,false);assert.equal("methods"in s,false);assert.equal("detailedResult"in s,false)});
test("summary is materially smaller than detailed result",()=>{const detail=arena({payload:Array.from({length:1000},(_,i)=>({i,value:"large payload"}))});assert.ok(serializedBytes(make())<serializedBytes(detail)/10)});
test("summary fingerprint prefixes are correct",()=>{const s=make();assert.equal(s.datasetFingerprintPrefix,"datasetabcde");assert.equal(s.resultFingerprintPrefix,"resultabcdef")});
test("unavailable summary metrics remain null",()=>{const a=arena();a.entries=[];a.performanceRanking=[];const s=createResearchSummary({runId:"VRX-aaaaaaaaaaaaaaaaaaaaaaaa",createdAt:"2026-01-01T00:00:00Z",timeframe:"180D",anchor:"2026-01-01T00:00:00Z",datasetFingerprint:"dataset",resultFingerprint:"result",durationMs:1,arena:a});assert.equal(s.researchScore,null);assert.equal(s.topStrategy,null)});
test("safe serialization never emits NaN or Infinity",()=>assert.deepEqual(sanitizeForJson({a:NaN,b:Infinity,c:-Infinity}),{a:null,b:null,c:null}));
