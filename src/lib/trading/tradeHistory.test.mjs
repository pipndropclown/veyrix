import assert from "node:assert/strict";import test from "node:test";import {createInitialPaperPortfolio} from "./paperPortfolio.ts";import {openMarketSpot} from "./multiMarketSpotEngine.ts";import {openFutures} from "./futuresEngine.ts";import {filterTradeHistory} from "./tradeHistory.ts";
let p=createInitialPaperPortfolio("2026-01-01");p=openMarketSpot(p,{marketId:"BTC",amountUsdc:100,price:100,source:"MANUAL",timestamp:"2026-01-01T00:00:00Z"}).portfolio;p=openMarketSpot(p,{marketId:"ETH",amountUsdc:100,price:100,source:"AUTONOMOUS",timestamp:"2026-01-01T00:01:00Z"}).portfolio;p=openMarketSpot(p,{marketId:"SOL",amountUsdc:100,price:100,source:"MANUAL",timestamp:"2026-01-01T00:02:00Z"}).portfolio;p=openFutures(p,{marketId:"BTC",side:"LONG",leverage:2,marginUsdc:100,price:100,stopLoss:null,takeProfit:null,timestamp:"2026-01-01T00:03:00Z"}).portfolio;
for(const id of ["BTC","ETH","SOL"])test(`${id} history filter`,()=>assert.ok(filterTradeHistory(p,{market:id,mode:"ALL",source:"ALL"}).every(t=>t.marketId===id)));
test("SPOT and FUTURES filters remain isolated",()=>{assert.ok(filterTradeHistory(p,{market:"ALL",mode:"SPOT",source:"ALL"}).every(t=>t.mode==="SPOT"));assert.ok(filterTradeHistory(p,{market:"ALL",mode:"FUTURES",source:"ALL"}).every(t=>t.mode==="FUTURES"))});
test("manual and autonomous history filters work",()=>assert.equal(filterTradeHistory(p,{market:"ALL",mode:"ALL",source:"AUTONOMOUS"}).length,1));
