import "server-only";
import { aggregateCandles, LIVE_TIMEFRAMES } from "./liveCandles";
import { mergeHistoricalChunks } from "@/lib/backtesting/marketCandles";
import { MARKET_REGISTRY, type MarketId } from "./marketRegistry";
import type { LiveCandleSet, LiveTimeframe } from "@/types/liveTrading";
const MAX_PROVIDER_CANDLES=300,DISPLAY_CANDLES=180;
const cache=new Map<string,{expiresAt:number;data:LiveCandleSet}>();
export async function getLiveCandles(timeframe:LiveTimeframe,marketId:MarketId="SOL",now=Date.now()):Promise<LiveCandleSet>{const definition=LIVE_TIMEFRAMES[timeframe],market=MARKET_REGISTRY[marketId],cacheKey=`${marketId}:${timeframe}:${Math.floor(now/Math.min(definition.refreshMs,30000))}`,cached=cache.get(cacheKey);if(cached&&cached.expiresAt>now)return cached.data;const sourceCount=Math.min(MAX_PROVIDER_CANDLES,DISPLAY_CANDLES*definition.aggregationFactor),end=new Date(now),start=new Date(now-sourceCount*definition.providerGranularitySeconds*1000),url=new URL(`https://api.exchange.coinbase.com/products/${market.providerProductId}/candles`);url.searchParams.set("granularity",String(definition.providerGranularitySeconds));url.searchParams.set("start",start.toISOString());url.searchParams.set("end",end.toISOString());const response=await fetch(url,{cache:"no-store",headers:{Accept:"application/json","User-Agent":"Veyrix-Paper-Terminal/1.3"},signal:AbortSignal.timeout(8000)});if(!response.ok)throw new Error(`Candle provider returned ${response.status}`);const raw:unknown=await response.json();if(!Array.isArray(raw)||raw.length>MAX_PROVIDER_CANDLES)throw new Error("Candle provider returned malformed data");const normalized=mergeHistoricalChunks([raw]).candles,candles=(definition.aggregationFactor>1?aggregateCandles(normalized,definition.seconds):normalized).slice(-DISPLAY_CANDLES);if(!candles.length)throw new Error("No valid candles available");const data:LiveCandleSet={marketId,pair:market.providerProductId,source:"Coinbase Exchange",timeframe,intervalSeconds:definition.seconds,fetchedAt:new Date(now).toISOString(),candles};for(const [key,value] of cache)if(value.expiresAt<=now)cache.delete(key);cache.set(cacheKey,{expiresAt:now+Math.min(definition.refreshMs,30000),data});return data}
